# verify-generator-assertions
Check the generator's own claims — the element a component renders, whether it may hold content, the source it cites, and the configuration it reads — against authorities that already exist, instead of asserting them.
