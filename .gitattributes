# Generated Kotlin (DaisyUI components, Heroicons, component tests).
# Collapsed by default in GitHub PR review and excluded from language statistics.
# The diff stays viewable on expand — that reviewability is the point of
# committing these files, so do NOT add -diff here.
lib/generated/** linguist-generated=true
