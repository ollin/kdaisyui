# a-join-marks-its-items-by-the-call

Inside daisyJoin's content, a component call DaisyUI documents as a join item emits join-item by the call itself — a scope with member overloads — so join-item cannot be written where no .join sets the corner-radius variables it reads.
