component: Button
cssPrefix: btn
htmlElement: BUTTON
textParam: true
extras:
  - name: disabled
    type: "Boolean"
    default: "false"
    apply: "if (disabled) this.disabled = true"
  - name: type
    type: "ButtonType?"
    default: "null"
    imports:
      - "kotlinx.html.ButtonType"
    apply: "if (type != null) this.type = type"
