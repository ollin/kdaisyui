# Dependency, plugin, tooling, and submodule versions live in gradle/libs.versions.toml.
# The PROJECT version lives here as the single central place; all modules and JReleaser
# read it. JReleaser tags the git history with this value at release time.
version=0.1.0
