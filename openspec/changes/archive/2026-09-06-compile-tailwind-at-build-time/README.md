# compile-tailwind-at-build-time

Tailwind variants of DaisyUI classes never compile in the browser-build setup the docs teach, so lg:btn-lg silently does nothing. Compile at build time in the example app, and document what library consumers must do in their own build.
