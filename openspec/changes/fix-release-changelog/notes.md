# Notes

## 1.1 — Reference changelog for `v0.1.2..v0.2.0`

Captured 2026-09-04 from `build/jreleaser/release/CHANGELOG.md`, generated by:

```
./gradlew jreleaserChangelog          # with JRELEASER_GITHUB_TOKEN=<any non-blank value>
```

HEAD was `b942d93`, project version `0.2.0`. The output ends at the `v0.2.0` tag — no commit
later than it appears — so this is the range that produced the published v0.2.0 notes, and no
detached checkout is needed.

**264 lines. This is the baseline every later task is measured against.**

### Structure

| Block | Lines | Entries | Verdict |
|---|---|---|---|
| `## 🚀 Features` | 4–11 | 3 | keep — this is the whole point of the changelog |
| `## 🧰 Tasks` | 14–121 | 108 | hide — dependency noise (task 2.2) |
| `## 📝 Documentation` | 124–125 | 2 | keep |
| unnamed, after `---` at 127 | 128–264 | 137 | hide — uncategorized (task 2.1) |

**The uncategorized bucket has no heading.** It renders as a bare `---` rule followed by the
commits. Searching for a `##` heading will not find it — worth knowing before task 2.1 tries to
confirm its removal.

### Counts differ from the proposal

The proposal says "117 uncategorized commits" and "~120 dependency lines". Measured: **137
uncategorized** and **108 dependency**. The proposal's argument is unaffected — both buckets are
overwhelming and both go — but the numbers in it are estimates, not measurements, and 2.1/2.2
should assert against the values here.

### What must survive — the acceptance criterion for 2.3

Three feature entries:

- `30b991d` 🚨 commit generated sources, adopt DaisyUI 5.7.16, and gate regeneration drift
- `d30a1b0` **coverage**: enforce a hard 100% line+branch coverage gate (#158)
- `66b62ef` **breadcrumbs**: add `Items` and `Item` subcomponents with structure wrappers

Two documentation entries:

- `28a6d73` **openspec**: add adapt-daisyui-5-6 change proposal (#159)
- `9b52a6d` **openspec**: add coverage and mutation testing change proposals (#101)

`30b991d` is the one named explicitly in task 2.4: it is the only Conventional Commit in the
range, so if hiding a category takes it out, the fix is worse than the defect.

### Defect 1 — duplicated dependency entries

Every Renovate bump appears twice, once as the squashed merge commit carrying the PR number and
once as the raw commit:

```
- 0034960 **deps**: Update dependency js-yaml to v5.3.0 (#230), closes #230
- 3a5b3f8 **deps**: Update dependency js-yaml to v5.3.0
```

This is what tempts a reader toward `skipMergeCommits = true` — the cure that would delete
`30b991d`, the one entry worth having. Task 2.5 exists to head that off.

### Defect 2 — the mangled breaking-change note

`30b991d`'s entry runs from line 4 to line 9. The commit body's `BREAKING CHANGE:` footer
absorbs everything after it, including a paragraph that has nothing to do with the breaking
change:

```
- 30b991d 🚨 commit generated sources, adopt DaisyUI 5.7.16, and gate regeneration drift - *TooltipVariant.Neutral is removed; DaisyUI dropped
tooltip-neutral. See "How to migrate" in README.md, which also covers
positional arguments shifting as generated signatures gain modifiers.

Four OpenSpec changes: commit-generated-sources, cleanup-dead-code,
rejoin-main, adapt-daisyui-5-6.*
```

The "Four OpenSpec changes" paragraph was never meant to be part of the migration note. Per the
Conventional Commits spec the footer runs to the end of the body, so this is not a JReleaser
defect and no configuration fixes it — it is a commit-authoring rule, which is why section 3
records it in the two skills instead of trying to configure it away.

## 1.2 / 1.3 — The category keys of the `conventional-commits` preset

Measured, not read: `./gradlew jreleaserConfig --rerun` dumps the fully resolved model, preset
included. Assumption 1 is discharged — the generator does reveal the keys, so the categories do
not have to be owned explicitly.

**`jreleaserConfig` needs more credentials than `jreleaserChangelog`.** It validates the whole
model, so blank `signing.pgp.secretKey` and `deploy.maven.mavenCentral.sonatype.username` /
`.password` abort it. Placeholders for all of them are enough; nothing is contacted.

| key | title | labels | order |
|---|---|---|---|
| `merge` | 🔀 Merge | merge | 0 |
| `features` | 🚀 Features | feat | 10 |
| `fixes` | 🐛 Fixes | fix | 20 |
| `changes` | 🔄️ Changes | revert, style, perf, refactor | 30 |
| `test` | 🧪 Tests | test | 35 |
| `tasks` | 🧰 Tasks | chore | 40 |
| `build` | 🛠 Build | test, build, ci | 50 |
| `docs` | 📝 Documentation | docs | 60 |

Labelers map a commit-message prefix to a label: `regex:^(?:chore(?:\(.*\))?!?):\s.*` → `chore`,
and so on for build, ci, docs, feat, fix, perf, refactor, revert, style, test. Two more map
`Merge pull` and `Merge branch` to `merge`.

### There is no dependency category

This refutes task 2.2 as written. A `chore(deps): Update …` commit is labelled `chore` — the
scope is not part of the labeler regex — and lands in `tasks`. The 108 dependency lines are the
entire content of 🧰 Tasks, and there is no narrower key to hide.

Two ways forward:

1. **Hide `tasks`.** One line. Also hides any future `chore:` commit that is not a dependency
   bump.
2. **Add a labeler matching `chore(deps)` plus a category for it, and hide that.** Precise, but
   introduces two config blocks and a custom category to maintain.

Taking (1). In this repository the project's own commits use Arlo notation and are uncategorized;
Conventional Commits appear only on merge commits, which are Renovate bumps plus the occasional
feature or docs MR. So `tasks` is empty of anything but dependencies today, and a chore worth
reading about is unlikely to be a chore at all. The residual risk is recorded here rather than
designed around: if a non-dependency `chore:` MR ever matters to a reader, this hides it, and the
fix is option (2).

## 2.1 — `hide { uncategorized = true }`

Measured against the reference above:

| Block | Before | After |
|---|---|---|
| 🚀 Features | 3 | 3 — `30b991d`, `d30a1b0`, `66b62ef` |
| 🧰 Tasks | 108 | 108 |
| 📝 Documentation | 2 | 2 |
| uncategorized | 137 | 0 |
| **total lines** | **264** | **125** |

Nothing that should have stayed was taken with it. The `---` at line 127 is gone along with the
commits it introduced.

**`jreleaserChangelog` is UP-TO-DATE after a config change.** Gradle does not see
`build.gradle.kts` as an input to it, so the first re-run reported success, regenerated nothing,
and left the previous output in place — which reads exactly like "the setting had no effect".
Pass `--rerun`. Every later measurement in this change needs it.

### A note on how 2.3 can be checked

There is no shell in this environment, so a byte-level `diff` between the reference and a later
run is not available. Task 2.3 is therefore verified against the table and the two lists above:
section counts, and the five entries that must survive by hash.
