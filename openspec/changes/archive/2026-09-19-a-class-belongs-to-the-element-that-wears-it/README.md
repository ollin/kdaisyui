# a-class-belongs-to-the-element-that-wears-it

Declare every DaisyUI class on the function whose element actually carries it, and guard it the way the element cross-check guards the component element.
