# All versions (dependencies, plugins, tooling, and the daisyui/heroicons codegen
# submodule tags) live in gradle/libs.versions.toml. The project version is derived
# by JReleaser from the git tag at release time; local builds use the fallback in
# each module's build script.
