# commit-generated-sources

Commit the generated DaisyUI component and Heroicon Kotlin sources to git so the library's API is readable without a build and so later codegen changes are provable by diff.
